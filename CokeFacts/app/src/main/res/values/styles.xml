<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FactsLinearLayout">
        <item name="android:background">@color/colorRedLite</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="FactsFrameLayout">
        <item name="android:layout_height">@dimen/none_size</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_weight">3</item>
    </style>

    <style name="ViewPager">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="ArrowImgButton">
        <item name="android:layout_height">@dimen/arrow_size_img_btn</item>
        <item name="android:layout_width">@dimen/arrow_size_img_btn</item>
        <item name="android:hint">@string/prev_hint</item>
    </style>

    <style name="CokeImg">
        <item name="android:layout_height">@dimen/none_size</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_weight">2</item>
    </style>

    <style name="CokeBackgroundImg">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:paddingBottom">@dimen/coke_start_img_padding_bottom</item>
        <item name="android:src">@drawable/coke_start</item>
    </style>


</resources>
