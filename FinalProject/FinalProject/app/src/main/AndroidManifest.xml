<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finalproject.course.finalproject">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">

            <!--
           <intent-filter>
              <action android:name="android.intent.action.MAIN" />

               <category android:name="android.intent.category.LAUNCHER" />
           </intent-filter>
            -->
        </activity>
        <activity android:name=".activities.ProductsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.ProductsContentProvider"
            android:authorities="com.finalproject.course.finalproject.data.ProductsContentProvider"
            android:exported="false" />

        <activity android:name=".activities.AddProductActivity"></activity>

        <!-- Google place API key
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDObahXPXOKU6vV2eUH55O7pITmrdKBKYk"/>-->
    </application>

</manifest>
